{
  "name": "asgard-source",
  "version": "1.0.0",
  "description": "Asgard Cloudflare Worker source",
  "type": "module",
  "scripts": {
    "lint": "eslint workers/",
    "lint:fix": "eslint workers/ --fix",
    "precheck": "npm run lint && echo '✅ Lint passed - safe to deploy'"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0"
  }
}
